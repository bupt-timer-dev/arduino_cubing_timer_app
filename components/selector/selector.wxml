<page-container round="true" position="top" show="{{show}}" bindenter="workaround" bindleave="leaveDrawer">
  <view class="type-selector">
    <button wx:for="{{range}}" id="{{index}}" bindtap="selectType">
      {{item}}
    </button>
  </view>
</page-container>